---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: batchreleases.rollouts.yuyy.com
spec:
  group: rollouts.yuyy.com
  names:
    kind: BatchRelease
    listKind: BatchReleaseList
    plural: batchreleases
    singular: batchrelease
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BatchRelease is the Schema for the batchreleases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BatchRelease
            properties:
              strategy:
                properties:
                  steps:
                    items:
                      properties:
                        replicas:
                          anyOf:
                          - type: integer
                          - type: string
                          x-kubernetes-int-or-string: true
                      required:
                      - replicas
                      type: object
                    type: array
                required:
                - steps
                type: object
              template:
                x-kubernetes-preserve-unknown-fields: true
              workloadRef:
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - strategy
            - template
            - workloadRef
            type: object
          status:
            description: status defines the observed state of BatchRelease
            properties:
              currentStepIndex:
                format: int32
                type: integer
              currentStepState:
                type: string
              lastUpdateTime:
                format: date-time
                type: string
              maxSurge:
                anyOf:
                - type: integer
                - type: string
                x-kubernetes-int-or-string: true
              maxUnavailable:
                anyOf:
                - type: integer
                - type: string
                x-kubernetes-int-or-string: true
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              reason:
                type: string
              updatedReadyReplicas:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
