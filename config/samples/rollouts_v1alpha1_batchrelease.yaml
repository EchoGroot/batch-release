apiVersion: rollouts.yuyy.com/v1alpha1
kind: BatchRelease
metadata:
  labels:
    app.kubernetes.io/name: batch-release-tmp
    app.kubernetes.io/managed-by: kustomize
  name: batchrelease-sample
spec:
  # TODO(user): Add fields here
